
const getSellerIds = async (req, res) => {
    try {
        const id =[];
        // const seller = 
    }
    catch(e) {
        console.log(e.message);
        res.status(400).json({message: 'Internal Server Error'});
    }
}